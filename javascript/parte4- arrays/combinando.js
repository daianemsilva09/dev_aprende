const numeros = [1,2,3,4,5];

const combinado = numeros.join('.');
console.log(combinado);

const frase = "ola bem vindo";
const resultado = frase.splint(' ');
console.log(resultado);

console.log(resultado.join('-'));
